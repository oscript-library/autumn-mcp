
&ПараметрИнструмента(ЭтоМассив = Истина, Тип = "integer", Обязательный = Истина)
Перем МассивЧисел; // Число

&Инструмент(Имя = "multiply", Описание = "Умножает массив чисел")
Процедура ПриСозданииОбъекта()
		
КонецПроцедуры

&ВыполнениеИнструмента
Функция ВывестиРезультатУмножения() Экспорт

	Результат = МассивЧисел[0];
	Для инд = 1 по МассивЧисел.ВГраница() Цикл
		Результат = Результат * МассивЧисел[инд];
	КонецЦикла;
	Возврат Результат;
	
КонецФункции